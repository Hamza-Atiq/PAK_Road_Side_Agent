// Placeholder — Week 1 finish-up will:
//   1. Generate types from backend OpenAPI via `openapi-typescript`
//   2. Wrap them in an axios client (auth refresh, error normalization)
//   3. Add a `useIncidentSocket(incidentId)` hook for WS subscriptions
//
// Until then, the existing axios calls in apps/admin/src/api/ are the reference.
export const PLACEHOLDER = "@roadside/api-client";
